<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>المسودات</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header>
    <div class="container header-flex">
      <h1>المسودات</h1>
      <a href="/blogs" class="btn">← الرجوع للمدونات</a>
      <a href="/logout" class="btn logout">تسجيل الخروج</a>
    </div>
  </header>

  <main class="container">
    <% if (posts.length === 0) { %>
      <p>لا توجد مسودات حالياً.</p>
    <% } else { %>
      <ul class="posts-list">
        <% posts.forEach(post => { %>
          <li class="post-card">
            <% if (post.firstImage) { %>
              <img src="<%= post.firstImage %>" alt="صورة المقال" />
            <% } else { %>
              <div class="no-image">لا توجد صورة</div>
            <% } %>

            <div class="post-content">
              <h2><%= post.title %></h2>
              <div class="actions">
                <a href="/publish/<%= blogId %>/<%= post.id %>" class="btn">نشر</a>
								 <a href="/delete/<%= blogId %>/<%= post.id %>" class="btn delete">❌ حذف</a>
              </div>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </main>
</body>
</html>